// Tipos de ingresar datos por el usuario
// Eston bloquean la ejecucion del navegador y no se podra hacer nada

alert('Hola gente');

// Retorna lo que el usuario ingre, como segundo parametro lo podemos meter un valor por defect
let nom = prompt('Cual es tu nombre?','Sin nombre');//puede que tengamos un string vacio o un null si cancela la ventana

const seleccion = confirm('Esta seguro de borrar esto: ');
// Nos regresa un True o False

// Estas ventanas vienen en el objeto window
// si lo ejecutamos en node en la terminal nos dara error
// En node existe (Esto trae algo parecido a window)
console.log(global);// Solo ejecutando en node funciona

// Hay que saber donde vamos a ejecutar JS

